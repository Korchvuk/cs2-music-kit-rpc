# CS2 Music Kit RPC

Простая программа на C# для отображения вашего музыкального набора (Music Kit) из Counter-Strike 2 в статусе Discord Rich Presence.

## 🚀 Особенности
* Отображение текущего музыкального набора (обложка + название).
* Статус матча: отображение счета (CT-T) и текущего режима игры.
* Автоматическое создание конфигурационного файла.
* Минимальное потребление ресурсов.

## 📁 Структура проекта
* `src/` — исходный код программы.
* `cfg/` — файл интеграции для CS2.
* `config.json` — настройки вашего Discord Client ID и ID музыкального набора.
* `MusicKitRPC.csproj` — файл проекта для сборки.

## 🛠 Инструкция по установке

### 1. Настройка Discord
1. Перейдите на [Discord Developer Portal](https://discord.com/developers/applications).
2. Создайте новое приложение.
3. В разделе **Rich Presence -> Art Assets** загрузите:
   * Логотип игры с именем `cs2`.
   * Обложки музыкальных наборов (имена файлов должны соответствовать их ID, например `82`).
4. Скопируйте ваш **Application ID**.

### 2. Настройка CS2 (GSI)
Для того чтобы игра передавала данные в программу:
1. Перейдите в папку: `...\Steam\steamapps\common\Counter-Strike Global Offensive\game\csgo\cfg\`.
2. Поместите туда файл из папки `cfg` этого репозитория (`gamestate_integration_discord.cfg`).

### 3. Настройка программы
1. Запустите собранный `.exe` один раз, чтобы создался `config.json`.
2. Откройте `config.json` и настройте его:
```json
{
  "DiscordClientId": "ВАШ_ID_ПРИЛОЖЕНИЯ",
  "MusicKitId": 82
}
